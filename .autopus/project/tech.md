# Tech Stack — LMF_all

> Auto-generated by `/auto setup`. Updated by `/auto sync`.

## 스택 한눈에

| 레이어 | 기술 | 버전 |
|---|---|---|
| Frontend Runtime | React | 19.x |
| Frontend Build | Vite | 6.x |
| Frontend HTTP Client | axios | 1.8.x |
| Frontend Animation | framer-motion | 12.4.x |
| Frontend Icons | lucide-react | 0.479.x |
| Backend Runtime | Node.js | (package.json에 명시 없음, 16+ 권장) |
| Backend Framework | Express | 4.21.x |
| Backend ODM | mongoose | 8.3.x |
| Backend LLM SDK | @anthropic-ai/sdk | 0.92.x |
| Backend Config | dotenv | 16.6.x |
| Backend Security | express-rate-limit | 7.5.x, cors |
| Database | MongoDB | (외부 설치, 7+ 호환) |
| LLM API | Anthropic Claude | claude-haiku-4-5 (env로 override 가능) |
| Data ETL | Python | 3.x (pdf/ 스크립트들) |
| Harness | Autopus-ADK | 0.4 |

## Build / Run 명령

### Backend

```bash
cd gui/backend
npm install
npm run dev    # nodemon
npm start      # node server.js (prod)
```

환경 변수 (gui/backend/.env):
- `PORT` (기본 5000)
- `MONGO_URI` (기본 `mongodb://127.0.0.1:27017/lab_accreditation`)
- `ANTHROPIC_API_KEY` (필수, LLM 라우트용)
- `LLM_MODEL` (기본 `claude-haiku-4-5`)
- `LLM_MAX_TOKENS` (기본 300)
- `LLM_RATE_LIMIT_PER_MIN` (기본 20)

### Frontend

```bash
cd gui/frontend
npm install
npm run dev      # Vite dev server, /api → http://localhost:5000 proxy
npm run build    # → dist/
npm run preview  # built artifact 로컬 확인
```

### Data Import (수동, 1회 또는 점검표 갱신 시)

```bash
node gui/backend/import-data.js
# 또는
python pdf/import_to_mongodb.py
```

## Testing

| 영역 | 도구 | 현재 상태 |
|---|---|---|
| Backend unit | (미도입) | 0개 — `methodology.enforce: false` |
| Backend integration | (미도입) | 0개 |
| Frontend unit | (미도입) | 0개 |
| Frontend E2E | Playwright (`.playwright-mcp/` 캐시 존재, 스크립트는 미커밋) | 수동 실행만 |

후속 SPEC: SPEC-TEST-INTRO-001 (가칭) — Vitest + supertest 도입 후 enforce 복원

## Linting / Format

설정된 lint/format 도구 없음. 향후 도입 후보:
- Backend: ESLint + Prettier
- Frontend: ESLint (vite 기본 규칙) + Prettier

## CI/CD

미구성. 모든 빌드/배포는 로컬 수동.

## Architecture Patterns

| 패턴 | 적용 위치 |
|---|---|
| MVC-lite | backend: routes/ (controllers) + models/ |
| SPA + REST | frontend는 단일 React 트리, backend는 무상태 REST |
| Static-after-build | backend가 `frontend/dist`를 정적 서빙 + catch-all로 SPA 라우팅 |
| Allowlist update | `routes/api.js`의 `ALLOWED_UPDATE_FIELDS` (mass-assignment 방어) |
| Regex escape | 사용자 검색어를 정규식으로 변환 시 escape (`api.js:54`) |
| Schema validation | `findByIdAndUpdate({ runValidators: true })` |
| Rate limiting | `routes/llm.js`만 분당 N회 제한 (LLM 비용 보호) |
| Body size limit | `express.json({ limit: '32kb' })` |

## Security Posture

- API key는 `.env`에서만 (gitignored)
- ANTHROPIC_API_KEY 누출 시 LLM 라우트로만 영향 (rate limit + 32KB body limit이 한도 캡)
- CORS: `app.use(cors())` 와일드카드 — 의원 내부망 가정. 외부 노출 시 origin 화이트리스트 필요
- 인증 부재: 모든 PATCH/POST 무방비 (SPEC-AUTH-001 후속 권장)
- MongoDB injection: regex escape 적용, query는 정적 필드만 사용

## 외부 시스템 연동 포인트

| 시스템 | 위치 | 호출 빈도 |
|---|---|---|
| MongoDB | `server.js`, `import-data.js`, 모든 라우트 | 요청당 1~수회 |
| Anthropic API | `routes/llm.js` | 사용자가 "수정사유 생성" 클릭 시만 |
| Vite dev proxy | `gui/frontend/vite.config.js` | dev 모드에서만 |

## 의존성 정책

- 새 npm 패키지 추가 시 SPEC 또는 commit body에 도입 이유 명시
- security advisory 발생 시 즉시 업데이트 (현재 npm audit: 0 vulnerabilities)
- Major 버전 업그레이드는 별도 SPEC 권장
