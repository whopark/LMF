# Directory Structure — LMF_all

> Auto-generated by `/auto setup`. Updated by `/auto sync`.

## 트리

```
e:/LMF_all/
├── gui/                              # 제품 코드 (React + Express)
│   ├── backend/
│   │   ├── server.js                 # Entry: Express app, mongoose connect
│   │   ├── routes/
│   │   │   ├── api.js                # filters, items, changes, PATCH (277줄)
│   │   │   └── llm.js                # POST /reason — Anthropic proxy + rate limit
│   │   ├── models/ChecklistItem.js   # Mongoose schema (단일)
│   │   ├── import-data.js            # JSON → MongoDB import (재실행 가능)
│   │   ├── format_changes.py         # 일회성 데이터 변환 스크립트
│   │   ├── package.json
│   │   ├── package-lock.json
│   │   └── .env.example
│   └── frontend/
│       ├── index.html
│       ├── vite.config.js            # /api → :5000 proxy
│       ├── package.json
│       └── src/
│           ├── main.jsx              # Entry: ReactDOM render
│           ├── App.jsx               # Top-level state + routing toggle
│           ├── App.css               # Style import index (18줄)
│           ├── index.css             # Global resets
│           ├── components/
│           │   ├── Sidebar.jsx       # 필터 + 모드 토글
│           │   ├── DashboardView.jsx # 카드 그리드
│           │   ├── HistoryView.jsx   # Compare + Track 서브뷰
│           │   └── ItemModal.jsx     # 상세 + 편집
│           ├── hooks/useFilters.js   # ⚠️ unused (dead code)
│           ├── utils/helpers.jsx     # getDisplayData, formatDescription, getTagClass
│           └── styles/               # 분할 CSS (SPEC-CLEANUP-001 P2)
│               ├── tokens.css        # palette CSS variables (30 vars)
│               ├── layout.css        # app shell (140줄)
│               ├── components.css    # reusable widgets (183줄)
│               └── views/
│                   ├── dashboard.css
│                   ├── modal.css
│                   ├── history.css
│                   └── compare-sbs.css
├── pdf/                              # 데이터 + ETL
│   ├── 01 검사실운영/ ~ 90 분자진단검사/   # 분류별 11개 디렉토리
│   │   └── 2020.pdf ~ 2024.pdf       # 연도별 원본 PDF
│   ├── checklist_items_final.json    # ⭐ MongoDB import 소스 (단일 진실)
│   ├── classification_map.json
│   ├── import_to_mongodb.py          # ⭐ 메인 import 스크립트
│   ├── import_verification_reports_v4.py  # ⭐ 현재 ETL (v4)
│   ├── verification_reports_v4.json  # v4 중간 산출물
│   ├── dashboard.py                  # Streamlit 대시보드
│   ├── CLAUDE.md                     # 데이터 디렉토리 자체 컨텍스트
│   └── PRD.md
├── mongodb_data/                     # 로컬 MongoDB 데이터 (gitignored)
├── autopus-adk/                      # ⚠️ 별도 git repo (제품 아님)
├── .autopus/                         # ADK 메타 (이 문서가 위치한 곳)
│   ├── specs/SPEC-CLEANUP-001/
│   └── project/                      # ← 본 디렉토리
├── .claude/, .codex/, .opencode/, .agents/, .gemini/  # 하네스 설정 (대부분 gitignored)
├── ARCHITECTURE.md                   # 도메인/의존성 맵
├── AGENTS.md, CLAUDE.md, DESIGN.md   # 하네스 컨텍스트
├── autopus.yaml                      # 하네스 설정
└── *.png                             # ⚠️ root 스크린샷 다수 (artifacts/로 이동 권장)
```

## 패키지 역할

| 패키지 | 역할 | 진입점 |
|---|---|---|
| `gui/backend` | API 서버 + MongoDB 브릿지 + Anthropic 프록시 | `node server.js` (port 5000) |
| `gui/frontend` | 사용자 UI (React SPA) | dev: `vite`, prod: backend가 `dist/` 정적 서빙 |
| `pdf/` | 데이터 ETL 파이프라인 | `python pdf/import_to_mongodb.py` (수동) |
| `autopus-adk/` | 개발 하네스 자체 (외부) | `auto.exe` |

## 파일 통계 (소스 파일만)

| 항목 | 값 |
|---|---|
| 최대 라인 수 (소스) | `views/compare-sbs.css` 275줄 |
| 평균 컴포넌트 크기 | 약 130줄 |
| 300줄 위반 | 0건 |
| 200줄 초과 (분할 권장) | 1건 (compare-sbs.css 275줄) |

## 진입점 요약

| 진입점 | 명령 | 의존 |
|---|---|---|
| Backend dev | `cd gui/backend && npm run dev` (nodemon) | MongoDB 실행 중 |
| Backend prod | `cd gui/backend && npm start` | dist/ 빌드 완료 |
| Frontend dev | `cd gui/frontend && npm run dev` | Backend 실행 중 (Vite proxy) |
| Frontend build | `cd gui/frontend && npm run build` | — |
| Data import | `node gui/backend/import-data.js` | MongoDB + checklist_items_final.json |

## 주의 사항

- `gui/frontend/src/hooks/useFilters.js`는 어떤 컴포넌트도 import 하지 않음 (dead code)
- `autopus-adk/`는 메타 repo가 추적하지 않음 (별도 `.git`)
