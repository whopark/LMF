# Architecture — LMF_all

> Auto-generated by `/auto setup`. Updated by `/auto sync`. 한국어 작성.

## Overview

3-tier 웹 애플리케이션이다. React SPA → Express API → MongoDB. Anthropic Claude(haiku 4.5)를 호출하여 연도 간 검사 항목 변경의 수정사유를 자동 생성한다. 제품 이름은 **우수검사실 신임인증 심사점검표 리뷰**.

## Domains

| Domain | Path | Role |
|---|---|---|
| Presentation | `gui/frontend` | React 19 SPA. Vite 빌드 산출물을 backend가 정적 서빙 |
| API | `gui/backend/routes` | Express 라우터: filters, items, changes (api.js) + LLM proxy (llm.js) |
| Persistence | `gui/backend/models` | Mongoose 스키마: `ChecklistItem` (단일 컬렉션) |
| Data Pipeline | `pdf/` | 연도별 PDF + JSON 원본 → MongoDB import (Python ETL 다수 + node import 스크립트) |
| Harness (외부) | `autopus-adk/` | Autopus-ADK 하네스 (별도 git repo, 제품 의존성 아님) |

## Dependency Graph

```
[Browser]
   ↓ HTTP
[gui/frontend SPA] ─── axios → /api/* ───┐
                                         ↓
                                  [gui/backend Express :5000]
                                         ├──→ [MongoDB :27017 lab_accreditation]
                                         └──→ [Anthropic API: claude-haiku-4-5]

[pdf/import_to_mongodb.py] ──── one-shot ETL ──→ [MongoDB lab_accreditation]
```

## Layered Boundaries

- `frontend ↔ backend`: HTTP only via `/api/*`. 개발 환경은 Vite proxy(`vite.config.js`), 프로덕션은 backend의 static serve + catch-all
- `backend ↔ MongoDB`: Mongoose ORM, 단일 컬렉션 `checklist_items`
- `backend ↔ Anthropic`: `@anthropic-ai/sdk`, `routes/llm.js`만 호출 (분당 rate limit 적용)
- `harness ↔ product`: 단방향 — 하네스(`.claude/`, `.codex/`, `.opencode/`, `autopus.yaml`)는 제품 코드 변경에 영향, 제품은 하네스에 의존하지 않음
- `pipeline ↔ product`: data import는 한 번 실행 후 MongoDB가 source of truth가 됨. PDF/JSON 원본은 재현/감사용으로만 보존

## Mount Order (server.js)

`require('dotenv').config()` → `cors()` → `express.json({ limit: '32kb' })` → `static('../frontend/dist')` → `/api` (api.js) → `/api/llm` (llm.js) → catch-all `GET *` → MongoDB connect → listen.

## Rule Compliance

| Rule | Status | 근거 |
|---|---|---|
| file-size-limit (300줄 하드 한계) | **PASS** | 최대 `views/compare-sbs.css` 275줄 (legacy CSS 삭제됨) |
| TDD enforce | **PASS** | `autopus.yaml: methodology.enforce: true`. Backend 14개 + Frontend 3개 테스트 (Vitest + supertest + RTL) |
| doc-storage | **PASS** | SPEC-CLEANUP-001은 backend+frontend+config 변경이라 root `.autopus/specs/` 사용 |
| language-policy | **PASS** | 코드 주석 en, 커밋 ko, 응답 ko |
| project-identity | **PASS** | 제품은 `gui/`이고 `.autopus/`는 ADK 하네스 메타 — 혼동하지 않음 |

## Known Tech Debt

| 항목 | 영향 | 상태 |
|---|---|---|
| ~~`gui/frontend/src/styles/legacy/` dead CSS~~ | ~~빌드 산출물 비대~~ | ✅ SPEC-DEAD-CSS-001 완료 |
| ~~`pdf/` ETL 버전 누적~~ | ~~어떤 게 진실인지 혼란~~ | ✅ SPEC-DATA-CLEANUP-001 완료 |
| ~~인증/권한 부재~~ | ~~PATCH 무방비~~ | ✅ SPEC-AUTH-001 완료 (API Key) |
| ~~자동화 테스트 0개~~ | ~~회귀 검증 수동 의존~~ | ✅ SPEC-TEST-INTRO-001 완료 (14개) |
| ~~인라인 hex 색상~~ | ~~디자인 시스템화 미완~~ | ✅ SPEC-DESIGN-TOKEN-001 완료 |

## Cross-References

- 정책 규칙: `.claude/rules/autopus/*.md`
- 완료 SPEC: `.autopus/specs/SPEC-CLEANUP-001/` (초기 위생 정리)
- 제품 컨텍스트: `.autopus/project/product.md`
- 디렉토리 구조: `.autopus/project/structure.md`
- 기술 스택: `.autopus/project/tech.md`
- Workspace 정책: `.autopus/project/workspace.md`
